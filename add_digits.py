#code to print the sum of digits from the given number
twoDigitNumber= input("Type a two digit number:")
firstDigit=twoDigitNumber[0]
secondDigit=twoDigitNumber[1]
result = int(firstDigit)+ int (secondDigit)
print( result)